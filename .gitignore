# Directory or symlink to the shared cargo cache.
/target/
/target
.demo-state/
.env.demo
*.log
.DS_Store

# env / runtime / db / scratch — never commit live secrets or runtime state.
# Exact `.env` (not `.env*`) so tracked .env.example / .env.canary.example stay tracked.
.env
sessions/
runs/
*.db
*.sqlite
.scratch/
.irin-worktree.env
.irin-worktree-base
.irin-receipts/
.irin-tools/

# DMG packaging artifacts and isolated test state (generated)
/packaging/artifacts/
/packaging/build/
/packaging/test-home/
/packaging/test-apps/
/packaging/receipts/
# Shared or local Tauri target (dir or symlink)
council-rs/warroom-tauri/src-tauri/target/
council-rs/warroom-tauri/src-tauri/target